create or replace view MARKETING_DATA_QUALITY_DB.MONITORING.DATA_FRESHNESS_RESULTS(
	TABLE_NAME,
	DATE_COLUMN,
	LATEST_RECORD_DATE,
	DAYS_DELAYED,
	STATUS
) as

SELECT
TABLE_NAME,
DATE_COLUMN,
LATEST_RECORD_DATE,
DAYS_DELAYED,
STATUS
FROM QUALITY.INT_FRESHNESS_CHECKS;
