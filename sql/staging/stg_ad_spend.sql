create or replace view MARKETING_DATA_QUALITY_DB.STAGING.STG_AD_SPEND(
	CAMPAIGN_ID,
	DATE,
	CHANNEL,
	SPEND,
	IMPRESSIONS,
	CLICKS
) as

SELECT
CAMPAIGN_ID,
DATE,
LOWER(TRIM(CHANNEL)) AS CHANNEL,
SPEND,
IMPRESSIONS,
CLICKS
FROM RAW.AD_SPEND;
